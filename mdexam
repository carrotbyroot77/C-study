#include <stdio.h>

int add(int num){
    int sum = 0;

    for (int i = 1; i <= num; ++i) {
        for (int k = 0; k < i; ++k) {
            sum = sum + 1;
        }
    }

    return sum;
}

int main(){
    int result = add(5);
    printf("1부터 5까지의 합 = %d\n", result);
    return 0;
}